"""
Input Plugins for RAG Document Processor.

Input plugins convert various file formats into .rag files that can be processed
by the core system. Each plugin handles its own dependencies and format-specific logic.

Available Input Plugins:
- csv: Convert CSV files to structured .rag format (example)
- pdf: Extract text from PDF documents (planned)
- docx: Extract text from Word documents (planned)
- html: Extract text from web pages (planned)
- notion: Export from Notion databases (planned)
- confluence: Export from Confluence pages (planned)
"""